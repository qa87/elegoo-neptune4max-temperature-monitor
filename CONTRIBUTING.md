# Руководство по участию в проекте - Elegoo Neptune 4 Max Temperature Monitor

## 🤝 Как внести вклад

Мы приветствуем вклад в развитие проекта! Есть несколько способов помочь:

### 🐛 Сообщение об ошибках

Если вы нашли ошибку:

1. **Проверьте существующие issues** - возможно, проблема уже известна
2. **Создайте новый issue** с подробным описанием:
   - Операционная система и версия
   - Версия Python
   - Модель принтера и версия прошивки
   - Шаги для воспроизведения ошибки
   - Ожидаемое и фактическое поведение
   - Логи программы

### 💡 Предложения улучшений

Если у вас есть идея для улучшения:

1. **Создайте issue** с типом "enhancement"
2. **Опишите предложение** подробно
3. **Объясните пользу** для пользователей
4. **Приложите примеры** если возможно

### 🔧 Исправления и новые функции

Если вы хотите внести код:

1. **Создайте fork** репозитория
2. **Создайте ветку** для ваших изменений
3. **Внесите изменения** следуя стандартам кода
4. **Протестируйте** ваши изменения
5. **Создайте Pull Request**

## 📋 Стандарты кода

### Python стиль

Мы следуем PEP 8:

```python
# Правильно
def calculate_temperature(current_temp, target_temp):
    """Вычисляет разность температур."""
    return target_temp - current_temp

# Неправильно
def calc_temp(currentTemp,targetTemp):
    return targetTemp-currentTemp
```

### Документация

Все функции должны иметь docstrings:

```python
def monitor_temperature(self, target_temp):
    """
    Мониторит температуру принтера.
    
    Args:
        target_temp (float): Целевая температура в градусах Цельсия
        
    Returns:
        bool: True если температура достигнута
        
    Raises:
        ConnectionError: Если не удается подключиться к принтеру
    """
    pass
```

### Комментарии

Комментарии должны объяснять "почему", а не "что":

```python
# Правильно - объясняет причину
# Проверяем каждые 5 секунд для баланса между
# точностью и производительностью
time.sleep(5)

# Неправильно - очевидное действие
# Увеличиваем счетчик
counter += 1
```

## 🧪 Тестирование

### Обязательные тесты

Перед созданием PR убедитесь, что:

1. **Код запускается** без ошибок
2. **Существующие функции** работают
3. **Новые функции** протестированы
4. **Обработка ошибок** работает корректно

### Тестирование интерфейса

```python
# Пример теста GUI
def test_button_click():
    """Тест нажатия кнопки."""
    app = PrinterMonitorGUI(tk.Tk())
    app.start_btn.invoke()
    assert app.monitoring is True
```

### Тестирование API

```python
# Пример теста API
def test_temperature_api():
    """Тест получения температуры."""
    mock_response = {
        'result': {
            'status': {
                'heater_bed': {
                    'temperature': 25.0,
                    'target': 60.0
                }
            }
        }
    }
    temp = parse_temperature(mock_response)
    assert temp == 25.0
```

## 📁 Структура проекта

```
max4/
├── max4_alarm.py          # Основной код программы
├── requirements.txt       # Зависимости Python
├── README.md             # Основная документация
├── TECHNICAL.md          # Техническая документация
├── INSTALL.md            # Инструкция по установке
├── EXAMPLES.md           # Примеры использования
├── FAQ.md                # Часто задаваемые вопросы
├── CONTRIBUTING.md       # Это руководство
├── LICENSE               # Лицензия MIT
└── tests/                # Папка с тестами (планируется)
    ├── test_gui.py
    ├── test_api.py
    └── test_config.py
```

## 🔄 Процесс разработки

### 1. Планирование

Перед началом работы:

1. **Создайте issue** для обсуждения
2. **Получите одобрение** от мейнтейнеров
3. **Спланируйте изменения** детально

### 2. Разработка

Во время разработки:

1. **Следуйте стандартам** кода
2. **Пишите тесты** для новых функций
3. **Обновляйте документацию**
4. **Коммитьте часто** с понятными сообщениями

### 3. Code Review

После создания PR:

1. **Проверьте CI/CD** результаты
2. **Отвечайте на комментарии** ревьюеров
3. **Исправляйте замечания**
4. **Обновляйте PR** при необходимости

### 4. Слияние

После одобрения:

1. **Слейте изменения** в main ветку
2. **Удалите feature ветку**
3. **Обновите документацию** при необходимости

## 📝 Коммиты

### Формат сообщений коммитов

Используйте следующий формат:

```
тип(область): краткое описание

Подробное описание изменений (если необходимо)

Fixes #issue_number
```

### Типы коммитов

- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `style`: форматирование кода
- `refactor`: рефакторинг
- `test`: добавление тестов
- `chore`: обновление зависимостей

### Примеры

```
feat(gui): добавить индикатор прогресса нагрева

Добавлен визуальный индикатор прогресса для отображения
близости к целевой температуре. Индикатор обновляется
каждые 5 секунд вместе с температурой.

Fixes #15
```

```
fix(api): исправить обработку ошибок соединения

Исправлена проблема с зависанием программы при потере
соединения с принтером. Добавлен тайм-аут для HTTP запросов.

Fixes #23
```

## 🏷️ Версионирование

Мы используем Semantic Versioning (SemVer):

- **MAJOR** (1.0.0): кардинальные изменения API
- **MINOR** (1.1.0): новые функции, обратно совместимые
- **PATCH** (1.0.1): исправления ошибок

### Changelog

Обновляйте CHANGELOG.md при каждом релизе:

```markdown
## [1.1.0] - 2024-01-15

### Added
- Индикатор прогресса нагрева
- Экспорт логов в файл

### Changed
- Улучшена обработка ошибок соединения

### Fixed
- Исправлена проблема с кодировкой в Windows
```

## 🔍 Code Review

### Что проверять

При ревью кода обращайте внимание на:

1. **Корректность логики** - код делает то, что должен
2. **Производительность** - нет ли узких мест
3. **Безопасность** - нет ли уязвимостей
4. **Читаемость** - понятен ли код
5. **Тестируемость** - можно ли протестировать
6. **Документация** - есть ли необходимые комментарии

### Комментарии в PR

Будьте конструктивными:

```
// Хорошо
Этот подход может быть медленным для больших данных. 
Рассмотрите использование генераторов.

// Плохо
Это плохо.
```

## 🚀 Релизы

### Подготовка к релизу

1. **Обновите версию** в коде
2. **Обновите CHANGELOG.md**
3. **Создайте тег** релиза
4. **Создайте GitHub Release**
5. **Обновите документацию**

### Тестирование релиза

Перед релизом протестируйте:

1. **Установку** с нуля
2. **Все основные функции**
3. **Обработку ошибок**
4. **Документацию**

## 📞 Поддержка

### Где получить помощь

1. **GitHub Issues** - для багов и предложений
2. **Discussions** - для вопросов и обсуждений
3. **Discord** - для быстрого общения
4. **Email** - для приватных вопросов

### Мейнтейнеры

Текущие мейнтейнеры проекта:

- [Имя] - основной разработчик
- [Имя] - документация и тестирование

### Поведение в сообществе

Мы придерживаемся принципов:

1. **Будьте уважительны** к другим участникам
2. **Будьте конструктивными** в обратной связи
3. **Будьте терпеливы** с новичками
4. **Будьте открыты** к новым идеям

## 📚 Дополнительные ресурсы

### Полезные ссылки

- [PEP 8 - Style Guide](https://pep8.org/)
- [Semantic Versioning](https://semver.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Python Testing](https://docs.python.org/3/library/unittest.html)

### Инструменты

- **Black** - форматирование кода
- **Flake8** - проверка стиля
- **Pytest** - тестирование
- **MyPy** - проверка типов

---

Спасибо за ваш вклад в проект! 🎉
